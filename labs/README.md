# Labs

Empty for now — Phase 1 seeds the first batch (IDOR, XSS, SSRF, Auth, one
Recon/OSINT challenge). See the [repo README](../README.md#roadmap) for the
plan and [SECURITY.md](../SECURITY.md) before running anything that lands
here.

Each category gets its own directory, one subfolder per lab:

```
labs/<category-slug>/<lab-slug>/
```
